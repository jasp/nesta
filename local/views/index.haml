%h1
  %img{ :src => "/logo.jpg", :alt => "brosemROBOT" }
  -#= @heading
  %small= @subtitle

#sidebar
  = haml :categories, :layout => false
  = haml :feed, :layout => false

#content
  - @articles.each do |article|
    = haml :summary, :layout => false, :locals => { :article => article, :heading => :h2 }
